# 搜索高度、在线覆盖与安全返航后续方案

R64矩阵已完成：[11轮报告](https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/docs/verification/r64_matrix/REPORT.md)。以下早期小节按历史阶段阅读；最新实施与剩余项见文末。

2026-09-09 R63更新：[本轮报告](https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/docs/verification/r63_recovery/REPORT.md)。原第三投恢复碰靶已修复并实际三次恢复通过；当前唯一已暴露的终局阻塞移至AUTO.LAND航向跳变及近地估计/接地阶段。以下R62问题描述保留为修复前记录，不代表R63仍未实施。

2026-09-09 更新至 R62 完整 seed11。本文为后续设计；本轮三次释放后恢复碰靶，完整 Gate FAIL，未启动十 seed。飞行代码保持冻结，未实施本文后续优化。执行顺序只在[ROADMAP](https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/VISION_2026_ROADMAP.md)维护：修复基线阻断并通过 seed11，再完成场景随机化，最后实施本文三项优化。

## 规则核对与返航目的地

本次实际翻阅用户本地`1786409924213228.pdf`（扫描版、19页），核对第11–13页评分及第15页流程；与`1779762009913486(1).pdf`对应文本交叉检查。页码均为PDF文件的物理页码。会议转写未找到明确放宽以下返航前提的答复。

| 条款 | 原文要点 | 本工程采用的解释 |
|---|---|---|
| 2.7比赛阶段(5)，PDF第15页 | “完成所有投放后”可选返回起飞点或穿越障碍区降落 | 不将未完成投放视作已获准进入走廊；未三投完成时，安全返航默认起飞H。这里“所有投放”指所带快递的投放完成，不要求每件都命中最高分 |
| 2.5计分规则(10)，第12页 | 可根据状态选择不越过障碍区、直接返回起飞区降落；没有后续任务得分 | 为放弃穿廊的回起飞区方案提供依据；不能给这一路线自动计满过门和终点降落分 |
| 2.5计分规则(6)，第11页 | 起飞悬停故障结束及出起降区后自主返航有单列处理 | 与正常投后放弃穿廊的条款分开解释，不能据此保证任意返回起飞区都有10分降落分 |
| 2.5计分标准(3)，第12–13页 | “上方翻越或水平飞行全程高于障碍物不得分” | 虚拟无限高柱可以防止跨越真实障碍，但不能让全程高于障碍的飞行自动获得避障分 |
| 2.7比赛阶段(3)/(4)，第15页 | 裁判发起飞指令即开始任务计时；30秒内不能起飞本轮0分；单次不超过10分钟 | 返航预算以同一比赛任务时钟为准，不能从三投开始或仅从搜索开始重新计时 |

原页：[第11页](rule_extracts/rulepage-11.png)、[第12页](rule_extracts/rulepage-12.png)、[第13页](rule_extracts/rulepage-13.png)、[第15页](rule_extracts/rulepage-15.png)。

“完成所有投放后”是正向前提，文本并非另外写了一句“少一投进走廊罚多少”；因此本方案采用保守且直接符合流程的任务分支，不将其夸大成已确认的额外处罚条款。正常三投完成且状态/时间足够时走廊终点降落；未投完、释放状态不确定或没有走廊时间余量时，走搜索区可达绕障路线回起飞H。不能越隔离墙抄近路，也不能为了进入走廊把未知释放伪装为投递完成。

当前代码实际上已有这种基本分流：`mission_core._return_action()`使用home_xy=(0,0)，常规第三槽完成恢复后才调用_post_delivery_route_action；LAND由bridge读取当前有效里程计位置进行交接。后续重点是完整验收起飞H分支、航向与时间预算，不是仅把一个“home”坐标改成走廊终点。完成安全回收与完整竞赛Gate PASS应分别记录。

## A. 障碍柱与搜索高度

思路可用，而且当前Planner已开启horizontal_avoidance：搜索区内有竖向结构支持的障碍，在规划占据层中从floor_z延伸到virtual_ceil_height。它相当于飞行范围内的“无限高柱”，不是修改真实点云、把树识别成数学无穷大。

当前条件：搜索区X=[−4.8,4.8]、Y=[−0.5,7.6]；障碍起始z=0.18local（0.40m AGL），支持点至少3个、邻域0.15m、竖向跨度至少0.20m；柱底−0.12local（0.10m AGL），顶部取当前规划顶界。普通3D占据仍保留。该实现识别的是受支持的竖向结构，不是专门的树语义分类器。

后续复用这一实现，把树箱的水平占据足迹及位置不确定度统一用于绕行，同时保持原始LIO/FreeDOM地图可读。柱顶延伸至有限飞行顶界即可，避免浮点inf或无谓扩大地图。柱子只限制XY穿越，不应让少量漂移点、树叶噪声永久堵死端点；占据支持、更新/消退与覆盖补扫共同验证。

提高高度要同时满足四项：真实比赛限高与机体上沿余量；明确体现低于障碍顶界的水平绕行过程；像素尺度/模糊/反光仍能支持粗检测；MID360仍能及时看到前方树箱。正装雷达位于机顶，高飞时地面及低处树箱可能更难进入有效视场，不能假设已建地图永不遗漏。

因此不把搜索全程直接改到树冠上方。先以当前1.40m FC AGL水平搜索作为基线；后续研究已确认自由区域内取景高度的适应性变化，接近靶标时的取景与低空绕障分阶段处理。高空规划即便绕过虚拟柱，也必须按真实轨迹判断避障得分；不能用虚拟障碍代替实际规则。

## B. 按相机视场在线调整遍历覆盖

复用Mission Manager维护的搜索计划和Planner执行链，不增加第二个目标发布者。输入是现场统一坐标后的边界/起降点、当前地图、真实机体pose和CameraInfo/安装TF；未知靶位、未知左右门口不能作为任务侧真值输入。

维护“已有效观察、尚未观察、当前不可达/遮挡”的地面覆盖网格。用图像曝光时刻的相机姿态将有效图像区域投到地面，再扣除墙/树遮挡、边缘质量差和图像积压影响。飞机飞过或航点完成，不自动等于下方地面已经观察；投递中断、重新规划和高度改变不能清空已有覆盖记忆。

按当前高度和航向的有效横向视野生成剩余区域航带：`航带间距 ≤ (1−重叠率) × 有效横向视野宽度`。相机是刚性安装，roll/pitch/yaw及16cm偏移一起参与投影；高度增加扩大几何视野，但目标像素变小，有效识别视野未必同比扩大。粗搜索覆盖与完整蓝环精修分开，不能要求搜索的每格都完整装下1m环。

根据当前占据地图选择可达端点、拆分被树箱截断的航带，对暂不可达区域保留补扫记录。高度稳定变化、新障碍阻断或明显覆盖缺口才更新剩余计划；当前安全短段保持连续，保留进度与重规划滞回，避免每帧改航线重新引入路线波动。

目标是三投完成前持续降低全场有效搜索域的未观察面积；三投完成后结束搜索。若时限/电量/不可达条件提前触发返航，报告未覆盖区域，不伪称全场覆盖或强行拖到失去返航余量。评价至少含有效覆盖面积、遗漏靶/重复覆盖、可达端点失败与搜索时间，不只看名义航程。

## C. 分线路的安全返航阈值

当前诊断配置 early_return_enabled=false，420秒提前返航关闭、搜索投递可持续到600秒总时限。保留但未用于提前返航的旧预算字段为forced_return_at=420s、return_land_reserve=180s。其他配置仍为总时限600s、motion_action_timeout=90s、delivery_reserve_per_slot=60s、nominal_speed=0.5m/s、path_factor=1.5、decision_guard=15s。核心默认510s不是当前全场配置值。

现有_delivery_eta()用到home的距离加统一return_land_reserve；不能用一个固定余量同时准确描述“到起飞H”和“廊外下降→入口→两门→终点H”。后续两条路线分别估时，并把当前释放/恢复事务尚需的时间一起计入。

R60历史成功样本的ROS时间如下，依据mission阶段与disarm状态事件；并非R61新地图测量：

| seed | 开始投后路线至disarm | LAND交接至disarm |
|---|---:|---:|
| 11 | 149.361s | 11.700s |
| 5 | 142.237s | 10.195s |
| 7 | 128.924s | 9.460s |

[原始提取结果](return_timing.json)。这说明仅按最后约10秒降落来留余量会严重低估总返航时间。三条成功记录不足以估计可靠P95尾部；失败/超时不能计为0或从统计里消失。起飞H的新返航路线尚无本场景完整记录，不能套用上述终点H耗时。

后续动态预算定义为：

`B_return = B_remaining_path + B_altitude_and_settle + B_H_capture_and_land + B_replan_and_error_margin`

`最晚退出搜索 = T_limit − B_return − B_current_uninterruptible_transaction`

接受一个新投递候选前还需满足：`当前时间 + 候选接近/对准/释放/恢复预算 + 对应返航预算 ≤ T_limit`。路径预算采用当前地图下的实际剩余绕障长度与保守有效速度，不只采用到终点的直线距离。下降、稳定、H重捕获、落地确认、必要重规划都要计入；发现地图不可达或电量条件不满足时提前退出。

起飞H线路的第一版候选预算为150s：沿用单段90s运动预算，再为H取景/下降/落地预留45s、余量15s，因此无进行中投递时的候选退出点为450s；若实际路径/高度/事务预算更大则更早返回。这是待验证的工程初值，不是基于新线路统计得到的最优值，也没有应用到当前代码。本批次420/180保持禁用；直到seed11/随机化阶段之后，起飞H线路完成独立实跑，再评估启用450/150或动态阈值。不得把该设计更新当作开启安全返航。

穿廊线路保留当前较长余量，基于多轮同一版本、同一路线的分阶段记录更新。样本少时用已观测最大值加工程余量；样本充分且保留独立验证集后，再使用高分位数及其不确定度。不能因成功样本快，就忽略墙距/跟踪失败而延后返航。

验收要分别覆盖：0/1/2投安全回起飞H、三投后正常穿廊、临近阈值且正在对准/释放/恢复、最长可达返航路径、H短时丢失以及预期时限内落地解除武装。起飞H目前贴边，也必须验证低空机架净空和H接近方式。无人机实际安全收尾可持续到比赛计时结束之后，但超时不得记作比赛PASS。


## R62 完整 seed11 带来的修订（已分析，尚未实施）

[本轮报告](https://github.com/Qinling-Melon-Farmers/liftrace-visionwork/blob/feat/r2026-competition-integrated/docs/verification/r62_full_seed11/REPORT.md)：`r2026_r62_complete_seed11_20260909_040421`，三槽回执成功，第三投恢复未完成，任务开始后462.893秒碰红十字靶，未进入走廊。只分析此轮，不继续seed1–10，也不以失败为由再开新轮次。

### 基线恢复的修复边界

当前控制交接阈值0.73 local，而桥的恢复确认阈值0.95 local；红十字内部爬升目标1.15 local。这三者含义不同，不能仅把0.73改成另一个数字就宣称解决。第三投461.103秒退出对准，旧规划设定值立即重新进入，随后跟随当前位置下沉，485.561秒碰靶。第一投ACK至恢复35.820秒，上一轮约37.431秒，也说明交接可能依赖动态过冲。

后续最小修复应复用现有控制状态机，明确“释放已确认→固定XY恢复爬升→达到统一恢复高度且稳定→允许新的规划命令”四步，确保每一步由现有链条中的一个明确模块控制；不要新建第二任务管理器。

- 恢复目标必须在安全范围内被控制器持续执行，桥确认条件与控制交接条件一致；不能在桥尚等高度时停止爬升。
- 接管必须区分下一任务的新轨迹与持续刷新的旧接近轨迹。不要仅凭话题消息新鲜就重放旧轨迹，也不要丢弃所有合法新轨迹；利用现有任务序号/规划目标生命周期实现。
- 需要保持时锁存固定空间目标；禁止每帧以当前位置重置悬停参考。轨迹偏离触发的保持也要有清晰的接管和退出条件，不能变成长期无位置恢复力的跟随。
- 保持释放一次性语义；成功ACK后的恢复不能要求再次出现完整靶环，也不得退回下降/重投。对标准靶和红十字都验证。
- 回归应覆盖较小爬升过冲、提前丢失视觉、旧轨迹持续发布、下一任务延迟、低空下降扰动；先验证固定恢复目标，再经用户新授权跑完整seed11。不能忽略接触或改小机体包络换取PASS。

### 取景与覆盖计划的新增输入

bridge于221.091秒已到达，221.105至319.811秒在视觉捕获/严格对准前等待98.706秒，规划接近不是主要耗时。250秒画面外圈底部出画，且飞机存在小范围绕动。保留本次录像回放，增加轻量JSON原因统计：中心偏差、完整外圈、稳定帧、曝光姿态、候选关联、门控拒绝；先确认主导因素，再在独立稳定段尝试取景高度或控制调整。不能直接放宽释放证据阈值。

本轮24点路线在第三投中断时index=16，未证明全场有效观察；实际最高FC高度1.749m，也不同于名义1.40m。在线覆盖计划应使用曝光时刻的完整刚体姿态、实际高度和可见地面，并为被裁切区域补扫；不要把完成航点计作足量视场覆盖。先冻结路线用于恢复回归，基线通过并完成随机化后再替换搜索策略。

### 返航预算的新限制

第三投回执距600秒任务上限只余162.955秒，且恢复尚未完成。本轮没有走廊或落地样本，不能用其拟合返航阈值；失败恢复时间是截尾样本，不是0秒。预算必须独立记录接近、有效取景、下降释放、ACK后恢复、进入走廊、过门、H捕获和落地。不要用最后一次落地的几秒取代整段预算。当前安全返航继续关闭，先解决上述基线问题；不在本轮更新任何生效阈值。


## R63 实施结果与下一步边界

已实施：控制恢复阈值统一为0.95local；投递对准交接后固定恢复目标，等新的任务运动命令；轨迹偏离时锁存保持位置，新轨迹才重置。三次恢复均约2–3秒，三投后9点路线和两门通过，未复现第三投下沉碰靶。保持当前已验证部分，不再改搜索/视觉来解释最后降落错误。

新阻塞：H连续10帧对准通过后AUTO.LAND激活时，PX4内部NED航向目标约90度跳到179度，历史heading reset计数未变，delta_heading约90度。优先针对本机PX4 switchTask无旧任务时的reset counters初始化作回归和最小修复；已有任务接续仍需保留真正的新估计重置处理。不能简单禁用所有EKF reset。固件侧修改与机载包必须标明依赖，不将SITL专用路径偷换成实机已修复。

同时验证LAND阶段航向连续、姿态与旋转净空、低空外部定位/气压计高度及接地稳定性。需记录AUTO.LAND前后内部trajectory_setpoint、姿态目标、重置计数和落地状态；目前已有ULog，下一轮无须全场bag。后续授权实跑仍以真实零碰撞、H落地、ON_GROUND和解除武装判定。

bridge本轮接近至释放6.244秒，不能因单轮较快删除R62的98.706秒视觉等待问题；保留录像回放任务。当前安全返航持续禁用，不调整生效预算。失败样本未落地，不能用本轮424.375秒当成功返航总耗时。固定seed11整场通过→随机化→自适应覆盖/返航的优先顺序不变。


## R64矩阵后的计划更新

已完成固件自动任务重置修复和固定seed11完整PASS；十seed原始7/10完整PASS，但5/7/8靶板压墙，不能视为10个合法场景样本。墙AABB排除已修并通过1000布局回归，seed11不变；旧记录不重写。

当前优先处理合法场景下seed3近地落地问题、近墙目标从规划安全接近点切换到视觉捕获的净空连续性。H稳定检测不等于整个机体已可安全落地；20cm空中停机未采用。场景随机化导出与四门组合几何已完成，真实点云未知门适应性下一阶段验收。

8个完整成功样本（含先导）从投后路线开始至disarm：128.830–181.580秒，中位146.235秒；LAND交接至disarm：12.930–15.389秒。旧180秒统一余量没有明显尾部裕度；后续必须按剩余真实绕障路线、下降和稳定过程分段估时。不得因移除5/7/8非法布设就删除其原始失败记录，也不能把未完成返程计0秒。

seed5故障返航到起飞H提供一条回收样本，返回指令505.706秒、disarm539.344秒，约33.638秒，但它不是常规未投完提前返航的统计验收。不能用单条快样本把原候选150秒大幅压低。当前420秒提前返航和新450/150方案都保持关闭；下一步仅设计和验证，不自动启用。


## R64时间与辅助相机的补充评审

见[本次计划](r64_time_camera/PLAN.md)。已有几何工具复用到R64实际布局；没有修改生成器、搜索算法、RKNN输入或返航阈值。新方案仍是计划，实施顺序以整机ROADMAP为准。
